<local:BaseAiView x:Class="WpfAiRunner.Views.RealEsrganView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WpfAiRunner.Views"
             Background="{StaticResource BgDark}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource BgCard}" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <DockPanel>
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="BtnLoadModel" Content="📂 Load Model (x4)" Click="BtnLoadModel_Click"/>
                    <CheckBox x:Name="ChkUseGpu" Content="Use GPU" Click="ChkUseGpu_Click" IsChecked="True"/>
                    <Button x:Name="BtnOpenImage" Content="🖼️ Open Image" IsEnabled="False" Click="BtnOpenImage_Click"/>

                    <Button x:Name="BtnUpscale" Content="✨ Upscale (x4)" Style="{StaticResource BtnPrimary}" 
                            IsEnabled="False" Click="BtnUpscale_Click" Width="120"/>

                    <Button x:Name="BtnSave" Content="💾 Save Image" IsEnabled="False" Click="BtnSave_Click"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <ProgressBar x:Name="PbarStatus" Width="100" Height="10" Margin="0,0,10,0" Visibility="Collapsed"/>
                    <TextBlock x:Name="TxtStatus" Text="Ready" VerticalAlignment="Center" Foreground="{StaticResource TextSecondary}"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardBorder}">
                <Grid>
                    <Image x:Name="ImgInput" Stretch="Uniform"/>
                    <TextBlock Text="Original" Foreground="#88FFFFFF" Margin="10" VerticalAlignment="Top" HorizontalAlignment="Left" FontWeight="Bold"/>
                </Grid>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource CardBorder}">
                <Grid>
                    <Image x:Name="ImgOutput" Stretch="Uniform"/>
                    <TextBlock Text="Real-ESRGAN x4" Foreground="#88FFFFFF" Margin="10" VerticalAlignment="Top" HorizontalAlignment="Left" FontWeight="Bold"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</local:BaseAiView>