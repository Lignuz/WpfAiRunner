<UserControl x:Class="WpfAiRunner.Views.RealEsrganView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="10">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="BtnLoadModel" Content="Load Model (x4)" Width="120" Margin="0,0,10,0" Click="BtnLoadModel_Click"/>

                <CheckBox x:Name="ChkUseGpu" Content="Use GPU" VerticalAlignment="Center" Margin="0,0,10,0" 
                          Click="ChkUseGpu_Click"/>

                <Button x:Name="BtnOpenImage" Content="Open Image" Width="100" Margin="0,0,10,0" IsEnabled="False" Click="BtnOpenImage_Click"/>
                <Button x:Name="BtnUpscale" Content="Upscale (x4)" Width="100" Margin="0,0,10,0" IsEnabled="False" Click="BtnUpscale_Click"/>

                <Button x:Name="BtnSave" Content="Save Image" Width="100" Margin="0,0,10,0" IsEnabled="False" Click="BtnSave_Click"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <ProgressBar x:Name="PbarUpscale" Width="150" Height="15" Margin="0,0,10,0" Visibility="Hidden"/>
                <TextBlock x:Name="TxtStatus" Text="Ready" VerticalAlignment="Center" Foreground="#AAA"/>
            </StackPanel>
        </DockPanel>

        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="#333" BorderThickness="1" Margin="0,0,5,0">
                <Grid>
                    <Image x:Name="ImgInput" Stretch="Uniform"/>
                    <TextBlock Text="Original" Foreground="White" Background="#88000000" VerticalAlignment="Top" HorizontalAlignment="Left" Padding="5"/>
                </Grid>
            </Border>

            <Border Grid.Column="1" BorderBrush="#333" BorderThickness="1" Margin="5,0,0,0">
                <Grid>
                    <Image x:Name="ImgOutput" Stretch="Uniform"/>
                    <TextBlock Text="Real-ESRGAN x4" Foreground="White" Background="#88000000" VerticalAlignment="Top" HorizontalAlignment="Left" Padding="5"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>