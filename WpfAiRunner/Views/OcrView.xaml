<local:BaseAiView x:Class="WpfAiRunner.Views.OcrView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WpfAiRunner.Views"
             Background="{StaticResource BgDark}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="150"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource BgCard}" CornerRadius="8" Padding="10" Margin="0,0,0,10">
            <DockPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Language:" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="{StaticResource TextSecondary}"/>
                    <ComboBox x:Name="CboLangSelect" Width="100" Margin="0,0,10,0" SelectionChanged="CboLangSelect_SelectionChanged" SelectedIndex="0">
                        <ComboBoxItem Content="Korean"/>
                        <ComboBoxItem Content="English"/>
                    </ComboBox>

                    <Button x:Name="BtnOpenImage" Content="🖼️ Open Image" Click="BtnOpenImage_Click" Margin="5,0"/>

                    <CheckBox x:Name="ChkShowBox" Content="Show Box" IsChecked="True" VerticalAlignment="Center" Margin="10,0" Click="ChkOption_Click"/>

                    <Button x:Name="BtnRun" Content="▶ Run OCR" Style="{StaticResource BtnPrimary}" Click="BtnRun_Click" Width="100"/>
                    <Button x:Name="BtnSave" Content="💾 Save" Click="BtnSave_Click" Margin="5,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <ProgressBar x:Name="PbarLoading" Width="100" Height="4" IsIndeterminate="True" Visibility="Collapsed" Margin="0,0,10,0"/>
                    <TextBlock x:Name="TxtStatus" Text="Ready" VerticalAlignment="Center" Foreground="{StaticResource TextSecondary}"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Style="{StaticResource CardBorder}">
                <Image x:Name="ImgInput" Stretch="Uniform"/>
            </Border>
            <Border Grid.Column="1" Style="{StaticResource CardBorder}">
                <Image x:Name="ImgOutput" Stretch="Uniform" MouseDown="ImgOutput_MouseDown" Cursor="Hand"/>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="{StaticResource BgCard}" CornerRadius="8" Margin="0,10,0,0">
            <Grid>
                <TextBox x:Name="TxtResult" IsReadOnly="True" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" 
                         Background="Transparent" Foreground="{StaticResource TextPrimary}" BorderThickness="0" Padding="10"
                         FontSize="14" TextWrapping="Wrap"/>
                <Button Content="Copy Text" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10" Click="BtnCopy_Click"/>
            </Grid>
        </Border>
    </Grid>
</local:BaseAiView>