<local:BaseAiView x:Class="WpfAiRunner.Views.RmbgView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:WpfAiRunner.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="{StaticResource BgDark}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource BgCard}" CornerRadius="8" Padding="10" Margin="0,0,0,10">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
                    <Button x:Name="BtnLoadModel" Content="📂 Load RMBG-1.4" Click="BtnLoadModel_Click"/>
                    <CheckBox x:Name="ChkUseGpu" Content="Use GPU" Click="ChkUseGpu_Click" IsChecked="True"/>
                    <Button x:Name="BtnOpenImage" Content="🖼️ Open Image" IsEnabled="False" Click="BtnOpenImage_Click"/>
                    <Button x:Name="BtnRun" Content="✨ Run" Style="{StaticResource BtnPrimary}" IsEnabled="False" Click="BtnRun_Click" Width="80"/>
                    <Button x:Name="BtnSave" Content="💾 Save" IsEnabled="False" Click="BtnSave_Click"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Threshold:" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="{StaticResource TextSecondary}"/>
                    <Slider x:Name="SldThreshold" Width="150" Minimum="0" Maximum="1" Value="0" SmallChange="0.1" LargeChange="0.2" VerticalAlignment="Center" Margin="0,0,20,0"/>

                    <TextBlock Text="Background:" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="{StaticResource TextSecondary}"/>
                    <ComboBox x:Name="CboBackground" Width="120" SelectedIndex="0" VerticalAlignment="Center">
                        <ComboBoxItem Content="Transparent"/>
                        <ComboBoxItem Content="White"/>
                        <ComboBoxItem Content="Black"/>
                        <ComboBoxItem Content="Green (Chroma)"/>
                        <ComboBoxItem Content="Blue"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <ProgressBar x:Name="PbarLoading" Width="100" Height="4" IsIndeterminate="True" 
                     Visibility="Collapsed" Margin="0,0,10,0"/>
                    <TextBlock x:Name="TxtStatus" Text="Ready" VerticalAlignment="Center" Foreground="{StaticResource TextSecondary}"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardBorder}">
                <Grid>
                    <Image x:Name="ImgInput" Stretch="Uniform"/>
                    <TextBlock Text="Original" Foreground="#88FFFFFF" Margin="10" VerticalAlignment="Top" HorizontalAlignment="Left" FontWeight="Bold"/>
                </Grid>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource CardBorder}" Background="{StaticResource CheckerboardBrush}">
                <Grid>
                    <Image x:Name="ImgOutput" Stretch="Uniform"/>
                    <TextBlock Text="Background Removed" Foreground="#88FFFFFF" Margin="10" VerticalAlignment="Top" HorizontalAlignment="Left" FontWeight="Bold"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</local:BaseAiView>