<local:BaseAiView x:Class="WpfAiRunner.Views.ColorizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WpfAiRunner.Views"
             Background="{StaticResource BgDark}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource BgCard}" CornerRadius="8" Padding="10" Margin="0,0,0,10">
            <DockPanel>
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="BtnLoadModel" Content="📂 Load DDColor" Click="BtnLoadModel_Click"/>
                    <CheckBox x:Name="ChkUseGpu" Content="Use GPU" Click="ChkUseGpu_Click"/>
                    <Button x:Name="BtnOpenImage" Content="🖼️ Open B/W Image" IsEnabled="False" Click="BtnOpenImage_Click"/>
                    <Button x:Name="BtnRun" Content="🎨 Colorize" Style="{StaticResource BtnPrimary}" 
                            IsEnabled="False" Click="BtnRun_Click" Width="100"/>
                    <Button x:Name="BtnSave" Content="💾 Save" IsEnabled="False" Click="BtnSave_Click"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <ProgressBar x:Name="PbarLoading" Width="100" Height="4" IsIndeterminate="True" Visibility="Collapsed" Margin="0,0,10,0"/>
                    <TextBlock x:Name="TxtStatus" Text="Ready" VerticalAlignment="Center" Foreground="{StaticResource TextSecondary}"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardBorder}">
                <Grid>
                    <Image x:Name="ImgInput" Stretch="Uniform"/>
                    <TextBlock Text="Original (B/W)" Foreground="#88FFFFFF" Margin="10" VerticalAlignment="Top" HorizontalAlignment="Left" FontWeight="Bold"/>
                </Grid>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource CardBorder}">
                <Grid>
                    <Image x:Name="ImgOutput" Stretch="Uniform"/>
                    <TextBlock Text="DDColor Result" Foreground="#88FFFFFF" Margin="10" VerticalAlignment="Top" HorizontalAlignment="Left" FontWeight="Bold"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</local:BaseAiView>