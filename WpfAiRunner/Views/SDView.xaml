<UserControl x:Class="WpfAiRunner.Views.SdView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgDark}" Loaded="UserControl_Loaded">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource BgCard}" CornerRadius="8" Padding="10" Margin="0,0,0,10" >
            <DockPanel>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="BtnPickModel" Content="📂 Model..." Click="BtnPickModel_Click"/>
                    <TextBlock x:Name="TxtModel" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="5,0,15,0" Text="(no model)"/>
                    <CheckBox x:Name="ChkUseGpu" Content="Use GPU (Required)" IsEnabled="False" IsChecked="True" VerticalAlignment="Center"/>
                    <Button x:Name="BtnRun" Content="▶ Generate" Style="{StaticResource BtnPrimary}" Click="BtnRun_Click" IsEnabled="False" Width="100" Margin="10,0,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Center" Margin="10,0,0,0">
                    <ProgressBar x:Name="PbarLoading" Width="100" Height="14" Minimum="0" Maximum="100" Visibility="Collapsed" Margin="0,0,10,0"/>
                    <TextBlock x:Name="TxtStatus" Foreground="{StaticResource AccentColor}" VerticalAlignment="Center" Text="Ready."/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardBorder}" Padding="10">
                <StackPanel>
                    <TextBlock Text="Prompt" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,5"/>
                    <TextBox x:Name="TxtPrompt" Height="80" TextWrapping="Wrap" Background="{StaticResource BgInput}" Foreground="{StaticResource TextPrimary}" Text="a cute cat"/>

                    <TextBlock Text="Negative" Foreground="{StaticResource TextPrimary}" Margin="0,15,0,5"/>
                    <TextBox x:Name="TxtNegative" Height="50" TextWrapping="Wrap" Background="{StaticResource BgInput}" Foreground="{StaticResource TextSecondary}"/>

                    <UniformGrid Columns="2" Margin="0,15,0,0">
                        <StackPanel Margin="0,0,5,0">
                            <TextBlock Text="Steps" Foreground="{StaticResource TextSecondary}"/>
                            <TextBox x:Name="TxtSteps" Text="20" Margin="0,5,0,0"/>
                        </StackPanel>
                        <StackPanel Margin="5,0,0,0">
                            <TextBlock Text="Seed" Foreground="{StaticResource TextSecondary}"/>
                            <TextBox x:Name="TxtSeed" Text="-1" Margin="0,5,0,0"/>
                        </StackPanel>
                    </UniformGrid>

                    <TextBlock Text="CFG Scale" Foreground="{StaticResource TextSecondary}" Margin="0,15,0,5"/>
                    <TextBox x:Name="TxtCfg" Text="7.5" Background="{StaticResource BgInput}"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardBorder}">
                <Grid>
                    <Image x:Name="ImgOutput" Stretch="Uniform" SnapsToDevicePixels="True"/>
                    <TextBlock x:Name="TxtPlaceholder" Text="Result will appear here" Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>