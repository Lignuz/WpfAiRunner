<UserControl x:Class="WpfAiRunner.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="{StaticResource BgDark}">

    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="WpfAiRunner" FontSize="48" FontWeight="Bold" 
                       Foreground="{StaticResource TextPrimary}" 
                       HorizontalAlignment="Center" Margin="0,0,0,10">
                <TextBlock.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="2" Opacity="0.5"/>
                </TextBlock.Effect>
            </TextBlock>

            <TextBlock Text="High-Performance Local AI Model Runner" 
                       FontSize="18" Foreground="{StaticResource AccentColor}" 
                       HorizontalAlignment="Center" Margin="0,0,0,30"/>

            <Border Background="{StaticResource BgCard}" CornerRadius="10" Padding="30" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Select a model from the left menu:" 
                               FontSize="16" Foreground="{StaticResource TextSecondary}" 
                               HorizontalAlignment="Center" Margin="0,0,0,15"/>

                    <UniformGrid Columns="2" HorizontalAlignment="Center">
                        <StackPanel Margin="10,0">
                            <TextBlock Text="• LaMa Inpainting" Foreground="{StaticResource TextPrimary}" Margin="0,4"/>
                            <TextBlock Text="• Depth Anything V2" Foreground="{StaticResource TextPrimary}" Margin="0,4"/>
                            <TextBlock Text="• Segment Anything (SAM)" Foreground="{StaticResource TextPrimary}" Margin="0,4"/>
                            <TextBlock Text="• Real-ESRGAN (Upscale)" Foreground="{StaticResource TextPrimary}" Margin="0,4"/>
                            <TextBlock Text="• RMBG-1.4 (Remove BG)" Foreground="{StaticResource TextPrimary}" Margin="0,4"/>
                        </StackPanel>
                        <StackPanel Margin="10,0">
                            <TextBlock Text="• Face Privacy (Mosaic)" Foreground="{StaticResource TextPrimary}" Margin="0,4"/>
                            <TextBlock Text="• AnimeGAN (Style Transfer)" Foreground="{StaticResource TextPrimary}" Margin="0,4"/>
                            <TextBlock Text="• Image Colorization" Foreground="{StaticResource TextPrimary}" Margin="0,4"/>
                            <TextBlock Text="• Stable Diffusion (GGUF)" Foreground="{StaticResource TextPrimary}" Margin="0,4"/>
                        </StackPanel>
                    </UniformGrid>
                </StackPanel>
            </Border>

            <TextBlock Text="Powered by ONNX Runtime, CUDA &amp; stable-diffusion.cpp (GGUF)" 
                       FontSize="12" Foreground="#666" 
                       HorizontalAlignment="Center" Margin="0,40,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>