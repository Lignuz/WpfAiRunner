<UserControl x:Class="WpfAiRunner.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1E1E1E">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="WpfAiRunner" FontSize="40" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
            <TextBlock Text="Select a model from the left menu to start." FontSize="18" Foreground="#888" HorizontalAlignment="Center" Margin="0,15,0,0"/>
            <TextBlock Text="Supports: LaMa Inpainting (CPU/GPU)" FontSize="14" Foreground="#555" HorizontalAlignment="Center" Margin="0,5,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>